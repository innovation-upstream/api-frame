load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/innovation-upstream/api-frame
# gazelle:go_naming_convention import_alias
# gazelle:go_naming_convention_external go_default_library
gazelle(
    name = "gazelle",
)
